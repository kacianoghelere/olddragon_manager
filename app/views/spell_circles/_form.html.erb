<%= form_for(@spell_circle) do |f| %>
  <% if @spell_circle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@spell_circle.errors.count, "error") %> prohibited this spell_circle from being saved:</h2>

      <ul>
      <% @spell_circle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :circle %><br>
    <%= f.number_field :circle %>
  </div>
  <div class="field">
    <%= f.label :spell_type_id %><br>
    <%= f.text_field :spell_type_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
