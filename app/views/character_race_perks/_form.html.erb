<%= form_for(@character_race_perk) do |f| %>
  <% if @character_race_perk.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@character_race_perk.errors.count, "error") %> prohibited this character_race_perk from being saved:</h2>

      <ul>
      <% @character_race_perk.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :character_race_id %><br>
    <%= f.text_field :character_race_id %>
  </div>
  <div class="field">
    <%= f.label :perk_id %><br>
    <%= f.text_field :perk_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
